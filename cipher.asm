org 100h

section .data
    ; [1. ส่วนของ นพรัตน์: ตัวแปรและ Buffer]
    ; (นพรัตน์ต้องเขียนส่งมาตรงนี้)

    ; [2. ส่วนของ อลงกต: ค่าคงที่]
    shift db 3

section .text
start:
    ; [3. ส่วนของ นพรัตน์: คำสั่งรับค่า]
    ; (นพรัตน์ต้องเขียนส่งมาตรงนี้)

    ; [4. ส่วนของ อลงกต: โครงสร้าง Loop หลัก]
    ; (คุณเขียนส่วนควบคุมการวนซ้ำตรงนี้)

main_loop:
    ; (ส่วนดึงข้อมูลจาก Buffer)

    ; [5. ส่วนของ ปุณณภา: ตัวกรองเงื่อนไข]
    ; (ปุณณภาต้องเขียนเช็คว่าตัวไหนควรเข้ารหัส ตัวไหนควรข้าม)

    ; [6. ส่วนของ จิรภัทร: สูตรคำนวณ]
    ; (จิรภัทรต้องเขียน Logic การบวก/ลบ ค่า Shift)

    ; [7. ส่วนของ นพรัตน์: คำสั่งแสดงผล]
    ; (นพรัตน์ต้องเขียนส่งมาตรงนี้)
